1
```
#include <string>
#include <vector>
#include <algorithm>
#include <set>
using namespace std;
bool visit[10];
set<string> s;
vector<vector<int>> result;
void dfs(int idx, int max)
{
    if(idx == max)
    {
        string temp = "";
        for(int i=0;i<10;i++)
        {
            if(visit[i] == 1) temp += to_string(i); // 유용함
        }
        s.insert(temp);
        return;
    }
    for(int i=0;i<result[idx].size();i++)
    {
        if(visit[result[idx][i]] != 1)
        {
            visit[result[idx][i]] = 1;
            dfs(idx+1,max);
            visit[result[idx][i]] = 0;
        }
    }
}
int solution(vector<string> user_id, vector<string> banned_id) {
    int answer = 1;
    for(int i=0;i<banned_id.size();i++)
    {
        vector<int> temp;
        for(int j=0;j<user_id.size();j++)
        {
            if(user_id[j].size() != banned_id[i].size()) continue;
            bool flag = true;
            for(int k=0;k<user_id[j].size();k++)
            {
                if(!(user_id[j][k] == banned_id[i][k] || banned_id[i][k] == '*'))
                {
                    flag = false;
                    break;
                }
            }
            if(flag == true) temp.push_back(j);
        }
        result.push_back(temp);
    }
    dfs(0,result.size());
    answer = s.size();
    return answer;
}
```
2
```

```
+ dfs사용하여 수열간 중복되는 원소 제거, 단어자체를 사용하지않고 index를 사용해야 간단하게 해결가능함.