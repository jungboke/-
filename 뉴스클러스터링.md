1
```
#include <string>
#include <cctype>
#include <vector>
#include <cmath>
using namespace std;

int solution(string str1, string str2) {
    int answer = 0;
    double answer2 = 0;
    vector<string> word1;
    vector<string> word2;
    for(int i=0;i<str1.size();i++)
    {
        if(isalpha(str1[i]) && isalpha(str1[i+1]))
        {
            if(str1[i] >= 'a')
            {
                char temp = str1[i] - 32;
                str1[i] = temp;
            }
            if(str1[i+1] >= 'a')
            {
                char temp = str1[i+1] - 32;
                str1[i+1] = temp;
            }
            word1.push_back(str1.substr(i,2));
        }
    }
    for(int i=0;i<str2.size();i++)
    {
        if(isalpha(str2[i]) && isalpha(str2[i+1]))
        {
            if(str2[i] >= 'a')
            {
                char temp = str2[i] - 32;
                str2[i] = temp;
            }
            if(str2[i+1] >= 'a')
            {
                char temp = str2[i+1] - 32;
                str2[i+1] = temp;
            }
            word2.push_back(str2.substr(i,2));
        }
    }
    int inter = 0;
    int sum = word1.size() + word2.size();
    for(int i=0;i<word1.size();i++)
    {
        for(int j=0;j<word2.size();j++)
        {
            if(word2[j] == word1[i])
            {
                inter++;
                word2.erase(word2.begin()+j);
                break;
            }
        }
    }
    if(word1.size() == 0 && word2.size() == 0)
    {
        return 65536;
    }
    sum = sum - inter;
    answer2 = double(inter) / double(sum);
    answer2 = answer2 * 65536;
    answer = floor(answer2);
    
    return answer;
}
```
2
```
#include <string>
#include <vector>
#include <cctype>
#include <algorithm>
#include <cmath>
using namespace std;

int solution(string str1, string str2) {
    int answer = 0;
    vector<string> one;
    vector<string> two;
    vector<string> inter;
    string temp = "";
    
    for(int i=0;i<str1.size()-1;i++)
    {
        if(isalpha(str1[i]) && isalpha(str1[i+1]))
        {
            str1[i] = tolower(str1[i]);
            str1[i+1] = tolower(str1[i+1]);
            temp = temp + str1[i] + str1[i+1];
            one.push_back(temp);
            temp = "";
        }
    }
    temp = "";
    for(int i=0;i<str2.size()-1;i++)
    {
        if(isalpha(str2[i]) && isalpha(str2[i+1]))
        {
            str2[i] = tolower(str2[i]);
            str2[i+1] = tolower(str2[i+1]);
            temp = temp + str2[i] + str2[i+1];
            two.push_back(temp);
            temp = "";
        }
    }
    vector<string> tmp_one = one;
    for(int i=0;i<two.size();i++)
    {
        if(find(tmp_one.begin(),tmp_one.end(),two[i]) != tmp_one.end())
        {
            inter.push_back(two[i]);
            tmp_one.erase(find(tmp_one.begin(),tmp_one.end(),two[i]));
        }
    }
    if(one.size() == 0 && two.size() == 0) return 65536;
    int inter_size = inter.size();
    int sum_size = one.size() + two.size() - inter_size;
    double num = double(inter_size) / double(sum_size);
    num = num*65536;
    answer = floor(num);
    return answer;
}
```
+ 0/1 정의가능한데 0/0정의안됨, int/int는 double형이 되지 않아 강제형변환필요. erase같은거할때 복사배열쓰는지 꼭확인하기, floor(), ceil() 알아두기, find(vector.begin(),vector,end())로 값찾기 가능